[project]
name = "kestra-python-template"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "kestra>=0.20.0",
    "python-dotenv>=1.0.1",
]
